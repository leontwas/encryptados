const config = {
  host: 'localhost',
  user: 'root',
  password: '', // si no configuraste contraseña para root en XAMPP
  database: 'encryptados',
  port: 3306 // puerto por defecto de MySQL en XAMPP
}

export default config